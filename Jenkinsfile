pipeline {
    stages {
        stage('Build') {
            steps {
                sh 'npm --version'
                sh 'echo "berhasil, yeayy"'
                sh 'npm install'
            }
        }
    }
}
